# Ozon Seller Bot

Telegram-бот для продавцов Ozon, помогающий планировать поставки, анализировать продажи и управлять товарными запасами.

## Возможности

*   **📈 Продажи**: Анализ динамики продаж, выполнение плана, конверсия (CR) и кликабельность (CTR).
*   **🏷️ Закупки**: Рекомендации по закупкам (сколько докупить товаров с учетом плана продаж).
*   **🚚 Отгрузки**: Расчет потребности по складам/кластерам (среднесуточный спрос, динамика).
*   **💰 Финансы**: Отчеты по транзакциям и начислениям.
*   **📢 Маркетинг**: Обзор рекламных кампаний.
*   **⚙️ Гибкая настройка**: Выбор методов прогнозирования (MA, ES), настройка горизонтов планирования.

## Установка

### Требования
*   Python 3.11+
*   Аккаунт Ozon Seller (Client ID + API Key)
*   Токен Telegram бота

### Локальный запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/your-repo/ozon-seller-bot.git
    cd ozon-seller-bot
    ```

2.  **Запустите скрипт установки:**
    ```bash
    python install.py
    ```
    Скрипт создаст виртуальное окружение (опционально), установит зависимости и создаст файл `.env`.

3.  **Настройте `.env`:**
    Откройте файл `.env` и укажите ваши ключи:
    ```ini
    TELEGRAM_TOKEN=ваш_токен_бота
    OZON_CLIENT_ID=ваш_client_id
    OZON_API_KEY=ваш_api_key
    ```

4.  **Запустите бота:**
    ```bash
    python bot.py
    ```

### Запуск в Docker

1.  **Соберите и запустите контейнер:**
    ```bash
    docker-compose up -d --build
    ```

## Структура проекта

*   `bot.py` — Точка входа.
*   `routers/` — Обработчики команд Telegram (меню, диалоги).
*   `modules_*/` — Бизнес-логика по доменам (продажи, финансы и т.д.).
*   `config_package/` — Управление конфигурацией и валидация настроек.
*   `data/` — Хранение локальных файлов (кэш, загрузки).

## Разработка

Для запуска тестов используйте:
```bash
pytest
```
